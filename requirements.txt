gpsd-py3>=0.3.0
pyyaml>=6.0
requests>=2.28
rich>=13
pygeopkg>=0.1.3

# Профиль экспорта "map" — интерактивная тепловая карта в HTML (src/wifi_heatmap).
# Тяжёлые научные пакеты (~400 МБ в сумме): IDW-интерполяция на сетку (scipy),
# проекция в метры (pyproj), растеризация слоёв (matplotlib/pillow),
# сборка leaflet-карты (folium/branca).
pandas>=2.2
numpy>=1.26
scipy>=1.11
folium>=0.16
branca>=0.7
matplotlib>=3.8
pyproj>=3.6
pillow>=10
